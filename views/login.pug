extends layout/main

block content
    .formWrapper
        form(action=routes.login, method="post")
            input(type="text",name="email", placeholder="이메일")
            input(type="password",name="password",placeholder="비밀번호")
            input(type="submit",value="로그인")
        include layout/socialLogin